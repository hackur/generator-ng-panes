'use strict';
var expect = require("chai").expect;
var Engines = require("../lib/engines.js");

describe("Engines", function()
{
	describe('#underscore' , function()
	{
		it("should replace the openning and closing tags" , function()
		{
			var source = "<% something %>";

			
		});
	});
});
